/** @type {import('tailwindcss').Config} */
export default {
  //specify the file format where tailwind should work
  content: ["./src/**/*.{vue, html, ts, js, tsx, jsx}"],
  theme: {
    extend: {},
  },
  plugins: [],
}

